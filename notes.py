"""
TODO: Create a letter using starting_letter.txt (Use starting_letter and invited names).

For each name in invited_names.txt, Replace the [name] placeholder with the actual name.
    1. Replace 1 name from invited_names.txt into starting_letter.
    2. Remove the spaces & finalise the letter
    3. Save the letter in the Output/ReadToSend folder.
    4. Repeat the process for all names

    EXTRA** Try to use a for loop; put all names in a list then replace [name]

readlines()
replace()
strip()

Final TODO
- Create empty letters
- append the new letters into each and relevant empty letter

"""